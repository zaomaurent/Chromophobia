from maps import maps
from math import radians
spawn_point = maps[1]["spawn point"]

players = {
    1: {
        "name": "Daniel",
        "spawn": spawn_point,
        "base rotation": radians(0)
    }
}